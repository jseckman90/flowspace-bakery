%h3= @oven.name

-# created a container div to house children cookie-info and prepare-btn divs. 
.container
  .cookie-info
    Cookie in oven:
    - if @oven.cookie
      1 cookie with 
    

    -# check if string is blank, if yes, display "no fillings". If no, display what the filling the user typed
    -# need to create logic to store any fillings string that is blank as the same string.  At the moment strings can be blank but can contain different amoutns of spaces, which then show as different cookies since technically the fillings ARE different
    - if @oven.cookie.fillings.blank?
      = "no fillings"
    - else
      = @oven.cookie.fillings

      -# check if string is blank, if yes, display "no fillings". If no, display what the filling the user typed
      - if @oven.cookie.fillings.blank?
        = "no fillings"
      - else
        = @oven.cookie.fillings

    
      
      - if @oven.cookie.ready?
        (Your Cookie is Ready)
        = button_to "Retrieve Cookie", empty_oven_path, class: 'button tiny'
    - else
      None

  %br

  .prepare-btn
    = link_to "Prepare Cookie", new_oven_cookies_path(@oven), class: 'button mobile-btn'  




